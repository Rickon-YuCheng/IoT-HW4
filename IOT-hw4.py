"""### 3. 用 Gradio 打造 Web App

我們先來安裝 `openai` 套件, 還有快速打造 Web App 的 `gradio`。
"""


import os

import gradio as gr
import requests

try:
    # 嘗試讀取同目錄的 .env，但沒有安裝 python-dotenv 也不會中斷
    from dotenv import load_dotenv
    load_dotenv()
except ImportError:
    pass
except Exception:
    # 若 .env 讀取有例外，保持靜默，避免影響主流程
    pass


system = """
你是一位「客觀的人工智慧隨身教授」，風格像台灣理工科系的指導教授，
並且帶有“研究室老闆”式的管理態度：講話直接、務實、不多餘，
認為學生是半個員工，應具備基本自主思考能力。

【你的定位】
1. 你不會教學生寫程式碼，也不會提供完整 code。
2. 你的責任在於：
   - 幫對方釐清問題本質
   - 提供架構、方向、策略、方法論
   - 指出關鍵盲點
   - 讓學生自己回去查、自己動手
3. 你認為「程式碼是學生要自己寫的」。

【回答範圍限制】
你只回答與人工智慧（AI）、機器學習（ML）、深度學習（DL）、資料科學相關的問題。  
包括：
- 模型設計與架構比較（Transformer、Mamba、CNN、RNN 等）
- 訓練流程、資料處理、評估方法
- 實務決策與 trade-off 分析
- 系統思考、pipeline 設計
- 模型部署與效能優化

若遇到 *非 AI、非技術、日常／閒聊／無意義問題*︰  
→ 你會用短、冷淡、很教授式的語氣回：「這不在我職責範圍，請問 AI 相關問題。」

【回答邏輯】
你會先判斷問題層級：

1. **亂問、不相關、無內容 → 超短回答**
   - 1～2 句話
   - 語氣冷靜但不失禮：「這與 AI 無關，我不處理。請問技術問題。」

2. **太空泛或根本沒想清楚 → 精簡要求補充資訊**
   - 指出缺了什麼背景（任務、資料、限制條件）
   - 像老闆一樣：「先把需求寫完整再問。」

3. **問得有價值、足夠具體的 AI 問題 → 一針見血回答**
   - 清楚點出問題本質
   - 說明該注意的核心因素（資料、架構、限制）
   - 分析幾個可行方向及各自取捨
   - 提出你會採用的策略與理由
   - 不提供 code，但會指出「該往哪查、該自己試什麼」

4. **學生追問 → 你才會更詳細解釋**
   - 深入講原理、架構差異、設計動機
   - 清楚條列步驟（高層次，不含程式碼）
   - 指出常犯錯誤與如何避免
   - 語氣仍偏客觀、像主管在教下屬

【語氣風格】
- 使用台灣用語。
- 像教授 + 老闆：理性、務實、耐心有限。
- 不廢話、不陪聊、不讚美、不情緒化。
- 對事不對人，但如果學生沒做功課，你會直接講。
- 對認真問問題的學生，你會給高密度、有價值的知識。

總則：
- 非 AI → 短拒絕
- 太空泛 → 要求補資訊
- 夠具體 → 一針見血的高層次架構指引
- 追問 → 才展開深入說明（不給程式碼）
"""



"""設定你要的模型。"""

# provider = "openai"
# model = "gpt-4o"

provider="groq"
model="llama-3.3-70b-versatile"

# provider = "groq"
# model = "gemma2-9b-it"

# provider = "groq"
# model = "openai/gpt-oss-120b"

if provider == "groq" and not os.getenv("GROQ_API_KEY"):
    raise RuntimeError("缺少 GROQ_API_KEY，請在環境變數或 .env 中設定你的 Groq 金鑰後再執行。")

GROQ_CHAT_URL = "https://api.groq.com/openai/v1/chat/completions"

def reply(system: str, prompt: str, provider: str, model: str) -> str:
    if provider != "groq":
        raise ValueError("目前僅支援 provider=='groq'。")

    api_key = os.getenv("GROQ_API_KEY")
    if not api_key:
        raise RuntimeError("缺少 GROQ_API_KEY，請在環境變數或 .env 中設定你的 Groq 金鑰後再執行。")

    payload = {
        "model": model,
        "messages": [
            {"role": "system", "content": system},
            {"role": "user", "content": prompt},
        ],
        "temperature": 0.7,
    }
    headers = {"Authorization": f"Bearer {api_key}"}

    resp = requests.post(GROQ_CHAT_URL, json=payload, headers=headers, timeout=30)
    resp.raise_for_status()
    data = resp.json()
    return data["choices"][0]["message"]["content"]

def lucky_post(prompt):
    response = reply(system=system,
                     prompt=prompt,
                     provider = provider,
                     model = model
                    )
    return response

with gr.Blocks(title="客觀的人工智慧隨身教授") as demo:
    gr.Markdown("### 🎓 客觀的人工智慧隨身教授")
    gr.Markdown(
        "這位教授專門回答與 **人工智慧 / 機器學習 / 深度學習 / 資料科學 / AI 實作** 相關的問題。\n\n"
        "- 如果你問的是 AI 相關問題：教授會以理性、直接、具體的方式給你建議。\n"
        "- 如果你問的是跟 AI 無關的問題：教授會像老闆一樣客觀地拒絕，並提醒你把問題改成與 AI 相關。"
    )


    with gr.Row():
        user_input = gr.Textbox(label="請輸入你想請教教授的 AI 問題：", placeholder="例如：我要如何在 Ubuntu 上手刻多模態模型用以實現沒有SourceCode paper")

    submit_btn = gr.Button("請教教授")
    output = gr.Textbox(label="教授的回覆", lines=10)

    submit_btn.click(fn=lucky_post, inputs=user_input, outputs=output)

demo.launch(share=True, debug=True)
